CREATE TABLE Shop (
	id BIGINT PRIMARY KEY,
	name VARCHAR(128) UNIQUE
);

CREATE TABLE ProductItem (
	id BIGINT PRIMARY KEY,
	shopId BIGINT FOREIGN KEY REFERENCES Shop(id),
	detailsUrl VARCHAR(1024) NOT NULL,
	keywordsBundle VARCHAR(1024) NOT NULL,
	priceInCent INTEGER NOT NULL
);
